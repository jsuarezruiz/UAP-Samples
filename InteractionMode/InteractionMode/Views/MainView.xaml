<base:PageBase
    x:Class="InteractionMode.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InteractionMode.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
    xmlns:base="using:InteractionMode.Views.Base"
    mc:Ignorable="d"
    DataContext="{Binding Path=MainViewModel, Source={StaticResource Locator}}">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock
            Text="{Binding InteractionMode}"
            FontSize="48"
            HorizontalAlignment="Center"
            VerticalAlignment="Center" />
    </Grid>
</base:PageBase>
